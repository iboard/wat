%strong= "Dear #{@user.name},"

%p 
  You've requeset to reset your password.
  You can do so by clicking the link below.

%blockquote
  = "Reset token: #{@user.password_reset_token}"
  %br/
  = link_to( @user.password_reset_token, reset_password_user_url(@user,@user.password_reset_token))

%pre
  = '----'
  = Settings.application_mail_signature 
