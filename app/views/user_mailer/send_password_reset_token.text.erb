ENGLISH VERSION (deutsche Version weiter unten)

Dear <%= @user.name -%>,

You've requeset to reset your password.
You can do so by clicking the link below.

Reset token: <%= @user.password_reset_token %>

  <%= reset_password_user_url(@user,@user.password_reset_token) %>

----
<%= Settings.application_mail_signature %>

DEUTSCHE VERSION

Lieber <%= @user.name %>!

Für Ihre email-addresse wurde ein Passwort-Reset-Token angefordert.
Um Ihr Passwort zurückzusetzen, klicken Sie bitte den hier angeführten Link.

  Reset token: <%= @user.password_reset_token %>
  <%= reset_password_user_url(@user,@user.password_reset_token) %>

----
<%= Settings.application_mail_signature %>
