var latest_events= '<center style="font-size: 0.75em; font-weight: 100;"><%= j Time.now().strftime("%H:%M.%S") -%></center>'+'<%= j render "events", events: @events -%>';
var interval_in_seconds = <%= (Settings.timeline.reload_interval || 10).seconds * 1000 %>;