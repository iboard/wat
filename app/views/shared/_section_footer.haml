.row
  - Section.all.each do |section|
    -if section.pages.any?
      .span2
        %h1=section.title
        %ul
          - section.pages.each do |page|
            %li=link_to page.title, page
      